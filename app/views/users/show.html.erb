<%= render partial: "shared/navbar2" %>

    <div class="jumbotron-artist">
        <div class="container"></div>
    </div>
    <br>
    <br>
    <br>

    <div class="container">
        <div class="row">
            <div class="col-md-6">
                <%=image_tag(current_user.image, size: '200x200', class: 'user-image') %>
            </h4>
        </div>
        <div class="col-md-6">
            <h3><%=current_user.name %>
                <h3>
                    <%= button_to "Open Spotify", current_user.account, method: :get, class: "", class: "btn btn-success btn-sm" %>
                    <br>
                    <h5>Nickname:
                        <%= current_user.nickname %>
                        | Followers:
                        <%= current_user.followers %>
                        | Email:
                        <%= current_user.email %>
                    </h5>
                    <h5>
                        <%= link_to "Your Top Artists Concerts", top_artists_path %>
                        |
                        <%= link_to "Your Top Tracks", top_tracks_path %>
                        |
                    <%= link_to "Search for concert", top_artists_path %>
                    </h5>

                </div>
            </div>
        </div>

        <br>

        <br>
        <div class="container">
            <center>
                <h2 class="featured">
                    <span>You Favorite Genres</span>
                </h2>
            </center>
        </div>
        <br>
        <div class="container">
            <div class="row">
                <% @top_artists.each do |artist| %>
                <div class="col-md-4">
                    <center><%= artist.genres.first %>
                        <center></div>
                        <% end %>
                    </div>
                </div>

                <br>

  <br>
