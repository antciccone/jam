<nav class="navbar navbar-default navbar-fixed-top" style="background-color: white">
    <div class="container-fluid">
      <div class="navbar-header">
          <a class="navbar-brand nav-logo" rel="home" href="#" title="Buy Sell Rent Everyting">
            <%#= image_tag("icon.png") %>          </a>
          <ul class="nav navbar-nav">
              <li class="">
                  <a class="jam" href="/">Jam</a>
              </li>

          </ul>
      </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                <% if current_user %>
                <li>
                    <p class="navbar-text">
                        <%=current_user.name %>
                    </p>
                </li>
                <li>
                    <p class="navbar-button">
                      <%= button_to "Logout", logout_path, method: :delete, class: "button1id", class: "btn btn-success btn-sm" %>
                    </p>
                </li>

                <% else %>
                <li>
                    <p class="navbar-text">
                      <%= link_to "Login with Spotify", spotify_login_path %>
                    </p>
                </li>
                <% end %>
            </div>
        </div>
    </nav>
    <div class="jumbotron-artist">
        <div class="container"></div>
    </div>
    <br>
    <br>
    <div class="container">
      <center><h2 class="featured"><span>20 Most Listen to Artists</span></h2></center>
    </div>
    <br>
    <br>

    <div class="container">
    <div class="row">
      <div class="col-sm-8">
        <div class="row">
          <% @top_artists.each do |artist| %>
          <div class="col-sm-6"><center><span class="artists-image"><%= image_tag(artist.images.first[:url], size: "360x275") %><center><span><br>
          </div>
          <% end %>
        </div>
      </div>

      <div class="col-sm-4">
        <center><p>All Artists</p><center>
          <hr>
        <div class="row">
          <% @top_artists.each do |artist| %>
          <div class="col-sm-4">
            <span class="artists-name"><%= link_to artist.name, ("#{concerts_path}""?name=#{artist.name}") %></span>
          </div>
          <% end %>
        </div>
      </div>

      <div class="col-sm-4 second-col">
        <center><p>Genres</p><center>
          <hr>
        <div class="row">
          <% @top_artists.each do |artist| %>
          <div class="col-sm-4"><span>  <%=  %>  <span><br>
          </div>
          <% end %>
        </div>
      </div>

  </div>
</div>
