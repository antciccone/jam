<nav class="navbar navbar-default navbar-fixed-top" style="background-color: white">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">Jam</a>
            <a class="navbar-brand" href="#">Concets in your area</a>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                <% if current_user %>
                <li>
                    <p class="navbar-text">
                        <%=current_user.name %>
                    </p>
                </li>
                <li>
                    <p class="navbar-text">
                      <%= link_to "Logout", logout_path, method: :delete %>
                    </p>
                </li>

                <% else %>
                <li>
                    <p class="navbar-text">
                      <%= link_to "Login with Spotify", spotify_login_path %>
                    </p>
                </li>
                <% end %>
            </div>
        </div>
    </nav>
    <div class="container">
      <div class="row">
        <% @top_artists.each do |artists| %>
        <div class="col-sm-4">
        <div class="artists">
          <center><span class="artists-name"><%= link_to artists.name, concerts_path %></span></center>
          <center><span class="artists-image"><%= image_tag(artists.images.first[:url], size: "250x250") %><span></center>
        </div>
        </div>
        <% end %>
      </div>
    </div>
