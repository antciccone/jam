<nav class="navbar navbar-default navbar-fixed-top" style="background-color: white">
    <div class="container-fluid">
      <div class="navbar-header">
          <a class="navbar-brand nav-logo" rel="home" href="#" title="Buy Sell Rent Everyting">
              <img style="max-width:35px; margin-top: -7px;" src="/assets/logo.png">
          </a>
          <ul class="nav navbar-nav">
              <li class="">
                  <a class="jam" href="/">Jam</a>
              </li>

          </ul>
      </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                <% if current_user %>
                <li>
                    <p class="navbar-text">
                        <%=current_user.name %>
                    </p>
                </li>
                <li>
                    <p class="navbar-button">
                      <%= button_to "Logout", logout_path, method: :delete, class: "button1id", class: "btn btn-success btn-sm" %>
                    </p>
                </li>

                <% else %>
                <li>
                    <p class="navbar-text">
                      <%= link_to "Login with Spotify", spotify_login_path %>
                    </p>
                </li>
                <% end %>
            </div>
        </div>
    </nav>
    <div class="jumbotron-artist">
        <div class="container"></div>
    </div>
    <br>
    <br>
    <div class="container">
      <center><h2 class="featured"><span>20 Most Listen to Artists</span></h2></center>
    </div>
    <br>
    <br>
    <div class="container">
      <div class="row">
        <% @top_artists.each do |artists| %>
        <div class="col-sm-4">
        <div class="artists">
          <span class="artists-name"><%= link_to artists.name, ("#{concerts_path}""?name=#{artists.name}") %></span>
          <span class="artists-image"><%= image_tag(artists.images.first[:url], size: "300x250") %><span>
        </div>
        </div>
        <% end %>
      </div>
    </div>
